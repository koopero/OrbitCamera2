horten:
  debug: true

control:
  url: ws://panjandrum.local:7777/

lights:
  path: control/lights

camera:
  record: 
    path: "/camera/$t.png"

manager:
  port: 7778


hss:
  port: 8765

mysql:
  path: /
  connection: 
    host:     localhost
    port:     8889
    user:     root
    password: root
    database: orbit2 
  columns:  [ "time", "number" ]
  table: state
  debug: false

osc:
  incoming: 9001
  outgoing: 9000
